---
    const {title} = Astro.props
---

<section>
    {title && <h2>{title}</h2>}
    <slot/> <!-- es lo que se le pasa al componente Section (es el hueco donde estará el contenido) -->
</section>

<style>
    section {
        /* centrar contenido */
        max-width: 700px;
        margin: 0 auto 48px;
    }

    h2 {
        margin-bottom: 8px;
        font-weight: 700;
        line-height 1.5;
        font-size: 1.5rem;
    }

    @media (width <= 768px) {
        h2 {
            text-align: center;
            font-size: 2rem;
        }
    }
    

</style>